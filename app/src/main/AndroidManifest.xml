<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="learn.idn.doazikirapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoaZikirApp">
        <activity android:name=".ui.PagiPetangDzikirActivity"></activity>
        <activity android:name=".ui.SetiapSaatDzikirActivity" />
        <activity android:name=".SetiapSaatActivity" />
        <activity android:name=".ui.QauliyahShalatActivity" />
        <activity android:name=".ui.detail.DzikirPetangActivity" />
        <activity android:name=".ui.detail.DzikirPagiActivity" />
        <activity android:name=".ui.detail.DetailArtikelActivity" />
        <activity android:name=".PagiPetangActivity" />
        <activity android:name=".ui.HarianDzikirDoaActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>